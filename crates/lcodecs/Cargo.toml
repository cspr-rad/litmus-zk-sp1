[package]
name = "lcodecs"
version = "0.1.0"
edition = "2021"

[dependencies]
lcrypto = { path = "../lcrypto" }
ltypes = { path = "../ltypes" }
lutils = { path = "../lutils" }
serde = { version = "1.0.214", features = ["derive"] }
