[package]
name = "ltypes"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.214", features = ["derive"] }
serde_bytes = "0.11.15"
lcrypto = { path = "../lcrypto" }
lutils = { path = "../lutils" }
hex = { version = "0.4.3", features = ["serde"] }
